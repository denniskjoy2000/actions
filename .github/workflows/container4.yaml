name: Container

on: push

jobs:
  node-docker:
    runs-on: ubuntu-latest
    container:
      image: denniskjoy/tfa:latest
      env:
        WDIR: /application
 #     options: -v .:/app
      volumes:
        - .:/application  
    steps:
      - name: call dockerfile
        run: |
          terraform init
